{
  "name": "wellframe-coach-mcp",
  "version": "0.1.0",
  "description": "MCP server exposing the Wellframe coach's tools over the user's local health database",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc",
    "pack": "npm run build && npx @anthropic-ai/mcpb pack . dist/wellframe-coach.mcpb",
    "validate": "npx @anthropic-ai/mcpb validate manifest.json"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "sql.js": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@anthropic-ai/mcpb": "^2.1.2",
    "@types/node": "^20",
    "@types/sql.js": "^1.4.11",
    "typescript": "^5.7.0"
  }
}
